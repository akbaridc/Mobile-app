<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bagusf.babeoapss">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".riwayat"></activity>
        <activity android:name=".activity_notifikasi" />
        <activity android:name=".activity_akun" />
        <activity android:name=".activity_keranjang" />
        <activity android:name=".activity_detail_produk" />
        <activity android:name=".akun.activity_registrasi" />
        <activity android:name=".Activity_kategori" />
        <activity android:name=".akun.LoginActivity" />
        <activity android:name=".JavaNavigation" />
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>